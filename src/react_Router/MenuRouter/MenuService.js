import React from 'react'

const MenuService=(props)=>{
    return(
        <>
            <h1 style={{color:"blue"}}>Hello Service page{props.name}</h1>
        </>
    );
}
export default MenuService;